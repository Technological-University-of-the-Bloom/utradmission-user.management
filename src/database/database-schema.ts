//import { serial, text, pgTable } from 'drizzle-orm/pg-core';
//import { rol } from './database-table-rol';
import { usuario } from './database-table-user';

export const databaseSchema = {
  usuario,
};
